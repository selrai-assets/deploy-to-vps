# deploy-to-vps

One skill to deploy any automation onto a Linux VPS (AWS EC2 via the `aws` CLI, CLI-first with browser fallback). Skill source in `skills/deploy-to-vps/`, research notes in `research/`.

## Agent skills

### Issue tracker

Issues live in Linear — team Core Builds (CORE), project "deploy-to-vps: battle test and harden". See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical labels, used as-is: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
